{
  "name": "matris-authentication-api",
  "version": "1.0.0",
  "description": "Library for Authentication endpoint.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "mocha -b -r ts-node/register test/unit/*.spec.ts test/unit/**/*.spec.ts test/unit/**/**/*.spec.ts",
    "test:data": "mocha -b -r ts-node/register test/data/**/*.spec.ts",
    "e2e": "mocha -b -r ts-node/register test/e2e/**/*.spec.ts",
    "check": "tslint -p tslint.json",
    "build": "npm run check && tsc",
    "build:clean": "rm -rf ./dist/* && npm run build"
  },
  "author": "Ensar Kovankaya <ensar@kovankaya.com> (http://kovankaya.com)",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "class-validator": "^0.9.1"
  },
  "devDependencies": {
    "@types/chai": "^4.1.4",
    "@types/express": "^4.16.0",
    "@types/mocha": "^5.2.5",
    "@types/node": "^10.5.3",
    "chai": "^4.1.2",
    "express": "^4.16.3",
    "matris-logger": "^1.1.4",
    "mocha": "^5.2.0",
    "ts-node": "^7.0.0",
    "tslint": "^5.11.0",
    "typescript": "^2.9.2"
  }
}
